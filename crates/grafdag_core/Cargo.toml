[package]
name = "grafdag_core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Document model, undo history and layout engine for grafdag"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
